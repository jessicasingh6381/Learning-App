<script setup lang="ts">
import AcademicNav from '@/Components/AcademicNav.vue'; import OwnershipBadge from '@/Components/OwnershipBadge.vue'; import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout.vue'; import { Head, Link, usePage } from '@inertiajs/vue3'; defineProps<{subjects:any[]}>(); const canManage=usePage<any>().props.auth.permissions.includes('subjects.manage');
</script>
<template><Head title="Subjects" /><AuthenticatedLayout><div class="d-flex justify-content-between mb-3"><div><h1 class="h2">Subjects</h1><p class="text-secondary">Stable platform defaults and tenant-created subject areas.</p></div><Link v-if="canManage" class="btn btn-primary align-self-start" :href="route('academic.subjects.create')">Add custom subject</Link></div><AcademicNav/><div v-if="!subjects.length" class="empty-state card">No subjects are available.</div><div v-else class="table-responsive card"><table class="table align-middle mb-0"><thead><tr><th>Order</th><th>Code</th><th>Name</th><th>Ownership</th><th>Status</th><th></th></tr></thead><tbody><tr v-for="subject in subjects" :key="subject.id"><td>{{subject.sort_order}}</td><td><code>{{subject.code}}</code></td><td>{{subject.name}}</td><td><OwnershipBadge :shared="subject.is_shared"/></td><td class="text-capitalize">{{subject.status}}</td><td class="text-end"><Link v-if="canManage&&!subject.is_shared" class="btn btn-sm btn-outline-secondary" :href="route('academic.subjects.edit',subject.id)">Edit</Link></td></tr></tbody></table></div></AuthenticatedLayout></template>
