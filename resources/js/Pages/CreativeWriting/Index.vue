<script setup lang="ts">
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout.vue';
import { formatDateOnly } from '@/Support/dateOnly';
import { Head, Link, router } from '@inertiajs/vue3';
const props=defineProps<{students:any[];selected_student_id:number|null;entries:any[];can_manage:boolean}>();
const filter=(event:Event)=>{const value=(event.target as HTMLSelectElement).value;router.get(route('creative-writing.index'),value?{student_id:value}:{},{preserveState:true});};
</script>
<template><Head title="Creative Writing Journal"/><AuthenticatedLayout><header class="d-flex justify-content-between align-items-start gap-3 flex-wrap"><div><p class="text-uppercase small fw-bold text-secondary mb-1">Supplemental writing</p><h1>Creative Writing Journal</h1><p class="text-secondary">Review students’ daily writing missions without changing ELAR sequencing.</p></div><Link class="btn btn-outline-primary" :href="route('creative-writing.prompts.index')">Prompt Library</Link></header><div class="card mt-4"><div class="card-body"><label class="form-label fw-bold" for="student-filter">Student</label><select id="student-filter" class="form-select" :value="selected_student_id||''" @change="filter"><option value="">All students</option><option v-for="student in students" :key="student.id" :value="student.id">{{ student.name }}</option></select></div></div><div class="card mt-4"><div class="card-body"><div v-if="!entries.length" class="empty-state">No creative-writing entries match this view yet.</div><div v-else class="table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Student</th><th>Prompt</th><th>Status</th><th>Words</th><th>Submitted</th><th></th></tr></thead><tbody><tr v-for="entry in entries" :key="entry.id"><td>{{ formatDateOnly(entry.date) }}</td><td>{{ entry.student }}</td><td>{{ entry.title }}</td><td><span class="badge text-bg-info text-capitalize">{{ entry.status.replace('_',' ') }}</span></td><td>{{ entry.word_count }}</td><td>{{ entry.submitted_at?new Date(entry.submitted_at).toLocaleString():'—' }}</td><td><Link :href="entry.url">Open</Link></td></tr></tbody></table></div></div></div></AuthenticatedLayout></template>
